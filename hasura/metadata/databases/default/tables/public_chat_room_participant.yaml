table:
  name: chat_room_participant
  schema: public
object_relationships:
- name: room
  using:
    foreign_key_constraint_on: room_id
remote_relationships:
- definition:
    hasura_fields:
    - user_id
    remote_field:
      user:
        arguments:
          where:
            id: $user_id
    remote_schema: CRM
  name: User
