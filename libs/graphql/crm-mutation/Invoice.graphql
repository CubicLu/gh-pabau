fragment InvoiceFields on Invoice {
  id
  customer_id
  guid
  date
  amount
  paid_amount
  discount_amount
  inv_total
  tip
  status
  location_name
  billers
  issue_to
}

fragment Invoice on Invoice {
  ...InvoiceFields
}
