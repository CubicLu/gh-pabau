query getPayments($contactID: Int!, $take: Int, $skip:Int){
  findManyPayments(contact_id: $contactID, take: $take, skip: $skip)
}

query totalPaymentsCount($ContactID: Int!){
  countPayments(contact_id: $ContactID)
}
