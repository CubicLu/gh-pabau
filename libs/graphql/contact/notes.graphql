query getClientHeader($contactId: Int!) {
  notes:findUniqueCmContact(where: {ID: $contactId}) {
    ID
    clientNotes:CmContactNote(orderBy: { CreatedDate: desc }) {
      ID
      Note
      CreatedDate
    	User {
        full_name
        image
      }
    }
    appointmentNotes:Booking(orderBy: { create_date: desc } ) {
      title
    }
    staffAlerts:CmContactAlert(orderBy: { CreatedDate: desc } ) {
      Note
      CreatedDate
      User {
        full_name
      }
    }
  }
}