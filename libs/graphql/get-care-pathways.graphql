query getCarePathways {
  findManyPathway(orderBy: { order: asc }) {
    id
    pathway_name
    description
    order
    _count {
      PathwayStep
    }
    Steps: PathwayStep {
      id
      name
      step
      order
    }
  }
}
