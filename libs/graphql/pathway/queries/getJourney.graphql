query getJourney($journey_id: Int!) {
  Journey: findFirstPathwaysTaken(
    where:{
      id:{
        equals:$journey_id
      }
    }
  ){
    id
    contact_id
    Pathway{
      id
      pathway_name
      steps: PathwayStep{
        id
        step
      }
    }
  }
}
