query getStages($company_id: Int) {
  stages: findManyLeadStatus(where: { company_id: { equals: $company_id } }) {
    id
    name: status_name
    order: status_order
    _count {
      CmLead
    }
  }
}
