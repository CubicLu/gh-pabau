query checkEmailLink($email: String) {
  contact: findFirstCmContact(where: { Email: { equals: $email } }) {
    ID
    Fname
    Lname
  }
  lead: findFirstCmLead(where: { Email: { equals: $email } }) {
    ID
    Fname
    Lname
  }
}
