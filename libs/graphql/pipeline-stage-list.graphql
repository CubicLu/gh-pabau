query getPipelineStages($company_id: Int, $pipeline_id: Int) {
  stages: findManyPipelineStage(
    where: {
      company_id: { equals: $company_id }
      pipeline_id: { equals: $pipeline_id }
    }
  ) {
    id
    company_id
    name
    _count {
      CmLead
    }
  }
}
