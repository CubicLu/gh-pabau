mutation ConnectVerifyCredentials($username: String!, $password: String!){
  ConnectVerifyCredentials(username:$username, password: $password){
    company_id
    contact_id
    fname
    lname
    email
  }
}
